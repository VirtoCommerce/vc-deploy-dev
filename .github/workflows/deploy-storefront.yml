name: vcptcore-stable storefront

on:
  workflow_dispatch:
  push:
    paths:
      - 'storefront/**'
    branches: [ vcptcore-stable ]

jobs:
  gitops:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    
    - name: Install vc-build
      run: |
        dotnet tool install --global VirtoCommerce.GlobalTool
    
    - name: Get storefront version
      id: platform-version
      run: |
        echo "STOREFRONT_TAG=$(cat ./storefront/image.json | jq -r '.Tag')" >> $GITHUB_ENV
        echo "STOREFRONT_REPO=$(cat ./storefront/image.json | jq -r '.Repository')" >> $GITHUB_ENV
      
    - name: Docker
      run: |
        docker login virtopaasregistrymain.azurecr.io -u vcpt-repo-saccess -p ${{ secrets.VCPT_ACR_DOCKER_PASSWORD }}
        docker pull $STOREFRONT_REPO:$STOREFRONT_TAG
        docker tag $STOREFRONT_REPO:$STOREFRONT_TAG virtopaasregistrymain.azurecr.io/vcpt/storefront:$STOREFRONT_TAG
        docker push virtopaasregistrymain.azurecr.io/vcpt/storefront:$STOREFRONT_TAG

    - name: Update app
      run: |
        vc-build SetHelmParameter -ArgoServer https://argocd.govirto.com -ArgoAppName vcptcore-stable -ArgoToken ${{ secrets.VCPTCORE_TOKEN }} -HelmParameters storefront.image.tag=$TAG
      env:
        TAG: ${{ env.STOREFRONT_TAG }}
