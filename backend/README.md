# How to update backed

1. Run powershell as administrator and run the following command:

```powershell
vc-build Update -edge -PackageManifestPath packages.json
```
2. Review the changes and commit to the repo. 

3. Commit the changes to the repo.